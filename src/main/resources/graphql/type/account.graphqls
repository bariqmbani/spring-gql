type Account {
    id: ID!
    name: String!
    accountNumber: String!
    balance: Float!
    customer: Customer!
    transactions: [TransactionEntry]!
}

extend type Query {
    account(id: ID!): Account
}
