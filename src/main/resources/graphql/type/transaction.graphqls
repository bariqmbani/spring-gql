type TransactionEntry {
    id: ID!
    description: String!
    amount: Float!
    rrn: String!
    timestamp: String!
    account: Account!
}

extend type Query {
    transaction(id: ID!): TransactionEntry
}
